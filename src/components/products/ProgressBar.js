const ProgressBar = () => {
  return (
    <div className="w-full bg-green-500 rounded">
      <div className="h-full ">
        <span>{`${20}%`}</span>
      </div>
    </div>
  );
};

export default ProgressBar;
